@model ProjectAkhirPAW.Models.Order

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Order</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdOrder" />
            <div class="form-group">
                <label asp-for="JumlahOrder" class="control-label"></label>
                <input asp-for="JumlahOrder" class="form-control" />
                <span asp-validation-for="JumlahOrder" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPelanggan" class="control-label"></label>
                <select asp-for="IdPelanggan" class="form-control" asp-items="ViewBag.IdPelanggan"></select>
                <span asp-validation-for="IdPelanggan" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPelayan" class="control-label"></label>
                <select asp-for="IdPelayan" class="form-control" asp-items="ViewBag.IdPelayan"></select>
                <span asp-validation-for="IdPelayan" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
