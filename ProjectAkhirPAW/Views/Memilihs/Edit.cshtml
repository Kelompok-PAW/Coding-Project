@model ProjectAkhirPAW.Models.Memilih

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Memilih</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdMemilih" />
            <div class="form-group">
                <label asp-for="IdMenu" class="control-label"></label>
                <select asp-for="IdMenu" class="form-control" asp-items="ViewBag.IdMenu"></select>
                <span asp-validation-for="IdMenu" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPelanggan" class="control-label"></label>
                <select asp-for="IdPelanggan" class="form-control" asp-items="ViewBag.IdPelanggan"></select>
                <span asp-validation-for="IdPelanggan" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="JumlahPesanan" class="control-label"></label>
                <input asp-for="JumlahPesanan" class="form-control" />
                <span asp-validation-for="JumlahPesanan" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
