@model IEnumerable<ProjectAkhirPAW.Models.Pelayan>

@{
    ViewData["Title"] = "Index";
}

<h2>Pelayan</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nama)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Alamat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JenisKelamin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nama)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alamat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JenisKelamin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPelayan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPelayan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPelayan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
